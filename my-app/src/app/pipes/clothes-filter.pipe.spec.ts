import { ClothesFilterPipe } from './clothes-filter.pipe';

describe('ClothesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClothesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
